#!/usr/local/env bash

source "./shared.sh"

# Runs a `fns` script locally within the repo on
# behalf of the `just` CLI
# Expects:
#   - $1 - path to script (e.g., "fns/ct/alpine")
#   - $2 - fully qualified path to repo root
function local_runner() {

    echo "TODO"

}
